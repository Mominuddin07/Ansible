<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>SJSU-{{ app_id }}</title>
    <style>
      body { font-family: system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, sans-serif; margin: 2rem; }
      .card { max-width: 800px; border: 1px solid #ddd; border-radius: 12px; padding: 2rem; box-shadow: 0 10px 25px rgba(0,0,0,0.05); }
      h1 { margin-top: 0; }
      .meta { color: #666; font-size: 0.9rem; }
      code { background: #f6f8fa; padding: 0.2rem 0.4rem; border-radius: 6px; }
    </style>
  </head>
  <body>
    <div class="card">
      <h1>Hello World from SJSU-{{ app_id }}</h1>
      <p class="meta">Served on port {{ web_port }} from {{ site_root }}</p>
    </div>
  </body>
</html>
